50. fix checkhealth issues, if any
51. add LSPs and others for JS, etc. https://github.com/williamboman/mason-lspconfig.nvim
52. configure zen mode -> remove lualine, change defaults, change lazy loading
54. remove conflicting keymaps which can be found in checkhealth (for which-key)
55. configure oil.nvim, vim-rhubarb doesn't work because of not working netrw -> fix netrw
57. fix r_language_server
58. configure which-key

Awesome Neovim:
- Lspsaga

final tweaks:
- clean up init.lua
- change tree structure (if not - move gitsigns to plugins.lua), set lazy load on events
- add description to keymaps
